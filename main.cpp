#include "main_triangle.cpp"
#include "main_app.cpp"
#include "main_demo.cpp"

int main(int, char**) {
    // main_demo(0, nullptr);
    main_app(0, nullptr);
    // main_triangle(0, nullptr);
    return 0;
}